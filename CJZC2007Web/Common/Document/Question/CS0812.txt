1）查询按钮有问题（不选类别，只选标题 查询不对）；（已解决）
2）没有数据的时候，不要显示“没有满足您查询条件的数据！”，直接隐藏“翻页控件”就可以了，可参考资产的查询；
3）信息维护页面，缺少查询功能；（已解决）
4）修改页面中没有使用FreeTextBox控件；在添加FreeTextBox出现了这样的错误：从客户端(remark="<P style="TEXT-INDEN...")中检测到有潜在危险的 Request.Form 值。 
说明: 请求验证过程检测到有潜在危险的客户端输入值，对请求的处理已经中止。该值可能指示危及应用程序安全的尝试，如跨站点的脚本攻击。通过在 Page 指令或 配置节中设置 validateRequest=false 可以禁用请求验证。但是，在这种情况下，强烈建议应用程序显式检查所有输入。  
